<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/ https://raw.githubusercontent.com/Gethe/wow-ui-source/live/Interface/AddOns/Blizzard_SharedXML/UI.xsd">
	<!-- The guide tab and its content are plain frames built with CreateFrame in Panel.lua. -->
	<!-- The tab reuses the stock LargeSideTabButtonTemplate directly (see Panel.lua CreateTab()) instead of a home-grown template here, and anchors to QuestMapFrame in Lua once Blizzard_WorldMap loads. -->
	<!-- The route pin below must be a real XML template: MapCanvasMixin AcquirePin resolves its template argument through CreateFramePool, which needs a declared virtual frame. -->
	<!-- A quest giver: the game's own available-quest "!", brightened on hover like the quest POIs. -->
	<Frame name="AdventureGuideForeverGiverPinTemplate" mixin="AdventureGuideForeverGiverPinMixin" enableMouse="true" virtual="true">
		<Size x="22" y="22"/>
		<Layers>
			<Layer level="ARTWORK">
				<Texture parentKey="Icon" atlas="QuestNormal" setAllPoints="true"/>
			</Layer>
			<Layer level="ARTWORK" textureSubLevel="1">
				<Texture parentKey="Glow" atlas="QuestNormal" alphaMode="ADD" setAllPoints="true" hidden="true"/>
			</Layer>
		</Layers>
	</Frame>
	<!-- A journey card: Blizzard's renown card (Blizzard_Journeys.xml:5-107, RenownCardButtonTemplate) at 0.77 scale,
	     its art and radial ring kept, the kind's quest-log icon in the ring, and a title, a count and a reason beside
	     it. AlphaHighlightButtonTemplate's OnLoad copies NormalTexture's atlas into the highlight, so the art is set
	     here, before the card is ever shown. Its pushed art is the same card: the renown pressed art is drawn off
	     true, so neither a press nor the chosen card ever moves. -->
	<Button name="AdventureGuideForeverJourneyCardTemplate" inherits="AlphaHighlightButtonTemplate" virtual="true">
		<Size x="288" y="86"/>
		<NormalTexture parentKey="NormalTexture" atlas="ui-journeys-renown-button"/>
		<PushedTexture parentKey="PushedTexture" atlas="ui-journeys-renown-button"/>
		<Frames>
			<Frame parentKey="IconFrame">
				<Size x="46" y="46"/>
				<Anchors>
					<Anchor point="LEFT" x="15"/>
				</Anchors>
				<Layers>
					<Layer level="OVERLAY" textureSubLevel="1">
						<Texture parentKey="Border" atlas="ui-journeys-renown-radial-bar" setAllPoints="true"/>
					</Layer>
					<Layer level="OVERLAY">
						<Texture parentKey="Icon">
							<Size x="18" y="18"/>
							<Anchors>
								<Anchor point="CENTER"/>
							</Anchors>
						</Texture>
					</Layer>
				</Layers>
			</Frame>
		</Frames>
		<Layers>
			<Layer level="OVERLAY">
				<FontString parentKey="Title" inherits="GameFontNormalMed2" justifyH="LEFT" wordwrap="false">
					<Size x="212" y="16"/>
					<Anchors>
						<Anchor point="TOPLEFT" relativeKey="$parent.IconFrame" relativePoint="TOPRIGHT" x="6" y="4"/>
					</Anchors>
				</FontString>
				<FontString parentKey="Subline" inherits="GameFontHighlightSmall" justifyH="LEFT" wordwrap="false">
					<Size x="212" y="14"/>
					<Anchors>
						<Anchor point="TOPLEFT" relativeKey="$parent.Title" relativePoint="BOTTOMLEFT" y="-2"/>
					</Anchors>
				</FontString>
				<!-- Shortest Path's minutes at the right of the subline's row; the subline stops short of them. -->
				<FontString parentKey="Travel" inherits="GameFontHighlightSmall" justifyH="RIGHT" wordwrap="false" hidden="true">
					<Size y="14"/>
					<Anchors>
						<Anchor point="TOPRIGHT" relativeKey="$parent.Title" relativePoint="BOTTOMRIGHT" x="-8" y="-2"/>
					</Anchors>
				</FontString>
				<FontString parentKey="Reason" inherits="GameFontHighlightSmall" justifyH="LEFT" wordwrap="false">
					<Size x="212" y="14"/>
					<Anchors>
						<Anchor point="TOPLEFT" relativeKey="$parent.Subline" relativePoint="BOTTOMLEFT" y="-2"/>
					</Anchors>
				</FontString>
				<!-- Blizzard's group quest tag (QUEST_TAG_ATLAS), at the right of the reason's row when a quest needs a group. -->
				<Texture parentKey="Group" atlas="questlog-questtypeicon-group" hidden="true">
					<Size x="12" y="12"/>
					<Anchors>
						<Anchor point="TOPRIGHT" relativeKey="$parent.Title" relativePoint="BOTTOMRIGHT" x="-8" y="-19"/>
					</Anchors>
				</Texture>
			</Layer>
		</Layers>
	</Button>
	<Frame name="AdventureGuideForeverPinTemplate" mixin="AdventureGuideForeverPinMixin" enableMouse="true" virtual="true">
		<Size x="26" y="26"/>
		<Layers>
			<!-- The panel's numbered ring, so a map pin reads as the same step as its route row. A dark disc, cut round
			     with the stock portrait mask, keeps the number legible over bright map art. -->
			<Layer level="BACKGROUND">
				<Texture parentKey="Disc">
					<Size x="22" y="22"/>
					<Anchors>
						<Anchor point="CENTER"/>
					</Anchors>
					<Color r="0" g="0" b="0" a="0.75"/>
				</Texture>
				<MaskTexture parentKey="DiscMask" file="Interface\CharacterFrame\TempPortraitAlphaMask" hWrapMode="CLAMPTOBLACKADDITIVE" vWrapMode="CLAMPTOBLACKADDITIVE">
					<Anchors>
						<Anchor point="TOPLEFT" relativeKey="$parent.Disc"/>
						<Anchor point="BOTTOMRIGHT" relativeKey="$parent.Disc"/>
					</Anchors>
					<MaskedTextures>
						<MaskedTexture childKey="Disc"/>
					</MaskedTextures>
				</MaskTexture>
			</Layer>
			<Layer level="ARTWORK">
				<Texture parentKey="Icon" atlas="adventureguide-ring" setAllPoints="true"/>
			</Layer>
			<Layer level="OVERLAY">
				<!-- Blizzard's bevelled gold numerals, each drawn centred in its own atlas box, so the digit sits exactly in
				     the ring whatever its width. Sized so the digit is about 11px tall. -->
				<Texture parentKey="Number">
					<Size x="22" y="25"/>
					<Anchors>
						<Anchor point="CENTER"/>
					</Anchors>
				</Texture>
			</Layer>
			<!-- The quest POI's own hover glow, over the ring; shown from OnMouseEnter since a Frame has no highlight state. -->
			<Layer level="ARTWORK" textureSubLevel="1">
				<Texture parentKey="Glow" atlas="UI-QuestPoi-InnerGlow" alphaMode="ADD" hidden="true">
					<Size x="30" y="30"/>
					<Anchors>
						<Anchor point="CENTER"/>
					</Anchors>
				</Texture>
			</Layer>
		</Layers>
		<!-- No OnEnter/OnLeave scripts: MapCanvas asserts a new pin has none and wires OnMouseEnter/OnMouseLeave itself. -->
		<!-- OnClick is not an XML script here: MapCanvasMixin OnPinMouseUp calls pin OnClick(button) directly once IsMouseClickEnabled() is true (see enableMouse above), same as OnMouseUp/Down. -->
	</Frame>
</Ui>
