# sift audit — Adventure Guide Forever @ a128f7a

Scope: whole repo. Gate before: pass. Gate after: pass (stylua, ruff, luacheck, LuaLS + multi-value
lint, model_spec 85 checks, actionlint/zizmor, gitleaks). First audit: nothing carried forward.

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven / left | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/` | 7/7 | 3 | 2 | 1 | 2 | 0 | pass |
| config-docs | 15/15 | 4 | 4 | 4 | 0 | 0 | pass |
| model (`Model.lua`, `Settings.lua`, `types/Namespace.lua`, spec) | 4/4 | 8 | 5 | 4 | 4 | 0 | pass |
| ui (`Pins.lua`, `Panel.lua`, `Panel.xml`, `Tracker.lua`) | 4/4 | 6 | 4 | 3 | 3 | 0 | pass |
| core (`Core.lua`, `State.lua`, `Integrations.lua`, `types/Client.lua`, TOC), two reviewers | 5/5 | 14 | 6 | 4 | 10 | 0 | pass |
| standards (`wow-forever-addon`) | 15/15 | 11 | — | 0 (proposals only) | 11 | 0 | — |

Evidence: luacheck 0, vulture 0, jscpd 0 clones, 0 `sift:` suppressions, `agents.py check` clean.

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| `tools/` | `ruff.toml` no longer cites a nonexistent `refresh-data.yml` | stale-docs | 0 |
| config-docs | `.pkgmeta` PLAN.md ignore (never existed); `.luacheckrc` Panel.lua globals under the right header; README detail example | dead-code / stale-docs | −1 |
| model | test-only `Model.Zones` deleted, spec asserts on `Plan(...).zones` (mutation-checked); impossible `not prefs or` branch; three stale annotations | dead-code / defensive-noise / stale-docs | −6 |
| ui | `Panel.xml` and `Pins.lua` stale comments; no-op `if not LayoutBlock then return end` | comment-narration / dead-code | −2 |
| core | impossible `if ns.RegisterSettings` guard; stale `SetSetting` comment; dead `AGFCheckButton`; `OpenPanel?` typed optional | defensive-noise / dead-code / stale-docs | −6 |

Net: −14 lines across 12 files (profile and this report excluded).

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Pins.lua:33-34` | The route pin tooltip prints `step.detail` then `step.reason`; `detail` starts as `reason` (`Model.lua:224-225`), so every pin shows its reason twice | Drop the `reason` line, or stop copying reason into detail |
| `AdventureGuideForever.toc` | No `X-Curse-Project-ID` / `X-Wago-ID` (WFA-16; skillup-forever has both), so a `v*` tag likely publishes to GitHub only | Add the IDs once the store projects exist |
| `State.lua:111` + `Core.lua:174` | Two stacked `C_Timer.After(0)` coalescers: a quest-log burst reaches a rebuild two frames later | Let one own coalescing |
| `State.lua:54` | If `GetAllCompletedQuestIDs` ever returns nil, only the next `PLAYER_ENTERING_WORLD` retries, so the route stays empty until a loading screen (the API is documented non-nil; unproven on Forever) | Retry on `QUEST_LOG_UPDATE` too, or drop the guard |
| `Core.lua:224` | `/agf` before `Blizzard_WorldMap` loads does nothing, silently (the compartment opens the map first) | Open the map first, as the compartment does |
| Copy (user-facing, proposals) | TOC Notes "3-5 quest steps" vs `MAX_STEPS = 9`; `docs/curseforge.md` "Go hands the step" vs a multi-stop route; `Settings.lua:46` says "chip" and "first time you open the guide" (it is first load); cog menu labels differ from the Settings page | Reword |

## Unproven candidates and findings left alone

| Location | Candidate | Why left / what would settle it |
|---|---|---|
| `Integrations.lua:67` | `Integrations.Cancel` uncalled | Live on `cb/journeys` (Menu.lua, Panel.lua) |
| `tools/ruff.toml:9` | `screenshots.py` ignore names a missing file | Live on `cb/journeys` |
| `Core.lua:23-24`, `types/Namespace.lua:58` | `legacy`, `professions` prefs never read | Proven dead, but they are SavedVariables defaults; removing them changes persisted data |
| `Core.lua:98` | Inline copy of `PREFS_DEFAULTS` | Unifying needs a deep copy (fresh `pinned`); not smaller |
| `Panel.lua:106,456` | Quests/Dungeons toggle written twice | `cb/journeys` moves the chips into the cog menu; unify there |
| `Model.lua:341` | `Phase()` classifies by key prefix | Needs a new step field; design change |
| `Model.lua:224` | `detail`/`reason` hold the same text | Tied to the tooltip bug above |
| `Model.lua:13` | `or 4` fallback | Depends on the host guaranteeing player level ≥ 0 |
| `Pins.lua:118,130,153,175` | Mouse handlers guard fields `OnAcquired` always sets | Fields are typed optional; removal cascades into types |
| `Pins.lua:161` | Scaling-level `-1` rule in three places | Would add a Model API |
| `tools/lint_multivalue.py:13` | `Token.kind` could be a `Literal` | Low value |
| Standards WFA-3/4/9/11/12/14/15 | No screenshots; options in two places; hand-made zone-card colours; CHANGELOG bullet form; CI runs only `agents.py check` | Assets and copy are proposals; WFA-12 drift is shared with skillup-forever (pack or siblings) |

## Learning captured

- Rules added: none — no repeated pattern a tool could recognise without judgment.
- Lenses added: none.
- Project skill: `cb/journeys` in-flight check, host-called method names, full OptionalDeps, `types/`
  zone, clone/standards evidence commands and the `ns\.` regex false positive, two anti-patterns.

## Next

Fix the duplicated pin-tooltip line and add the store IDs to the TOC before the first `v*` tag.
