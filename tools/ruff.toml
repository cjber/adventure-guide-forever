# The data generators and CI helpers are standard-library scripts run on the
# system python3.
line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "B"]

[lint.per-file-ignores]
# Imports wowmock after putting its directory on sys.path.
"screenshots.py" = ["E402"]
